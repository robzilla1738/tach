// The file the simulated agent edits. It starts "broken": `check.sh` looks for a
// pass marker and fails until the agent adds it. Real source, real edit, real
// command — only the command itself is a cheap script instead of a full build.

pub fn answer() -> i32 {
    // TODO: the agent corrects this and adds the pass marker.
    0
}
